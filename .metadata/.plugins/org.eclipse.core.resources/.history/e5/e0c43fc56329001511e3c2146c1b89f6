/*	Matthew S. Coley
 * 	Ch 12 debugs
 * 	13 July 2015
 */

package edu.nash.debug;

// This class determines the logarithm of a number
public class DebugTwelve1
{
  public static void main(String[] args) throws ArithmeticException
  {
     double num = -8.8, result;
     double test = 8.8, testResult;
     testResult = Math.log(test);
     System.out.println(testResult)
     try
     {
        if(num <= 0)
           throw(new ArithmeticException());
        result = Math.log(num);
        System.out.println("Result is " + result);
     }
     catch(ArithmeticException numOutOfRange)
     {
        System.out.println("Can't take logarithm for value of zero or lower");
     }
  }
}